#include <stdio.h>
/**
 * main - looka
 * @void: got the
 * header: klom and
 * Return: return zero
 */
int main(void)
{
char ch = 'a';
while (ch <= 'z')
{
putchar(ch);
ch = ch + 1;
}
putchar('\n');
return (0);
}
