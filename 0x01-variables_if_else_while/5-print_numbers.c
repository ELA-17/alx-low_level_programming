#include <stdio.h>
/**
 * main - starting with zero
 * @void: take input from the function
 * header: return from zero to ten
 * Return: return zero
 */

int main(void)
{
int num = 0;
while (num < 10)
{
printf("%d", num);
num = num + 1;
}
printf("\n");
return (0);
}
