#include "main.h"
/**
 * _abs - look for absolute value
 * @n: any number that can be converted to positive
 *
 * Return: return n or -n
 */
int _abs(int n)
{
if (n > 0)
return (n);
else
return (-n);
}
